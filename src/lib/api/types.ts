export type ApiVersion = {
    major: number;
    minor: number;
}

export type OrganizationData = {
    id: string;
    name: string;
    slug: string;
    createdAt: string;
    updatedAt: string;
}